# Distributed Job Scheduler

A .NET-based job scheduling system that allows you to schedule and execute tasks programmatically.
Supports cron expressions, HTTP requests, shell commands, and provides a REST API for job management.